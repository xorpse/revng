[package]
name = "revng-rust-lifter-example"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
cxx = "1"

[build-dependencies]
cc = "1"
cxx-build = "1"
revng-build = { path = "../../rust/revng-build" }
