[package]
name = "revng-sys"
version = "0.1.0"
edition = "2024"
description = "Low-level FFI bindings to revng's PipelineC interface"
links = "revng"
license = "MIT"
publish = false
include = ["/build.rs", "/src"]

[build-dependencies]
revng-build = { version = "0.1", path = "../revng-build" }
