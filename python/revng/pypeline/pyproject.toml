#
# This file is distributed under the MIT License. See LICENSE.md for details.
#

[project]
name = "pypeline"
dynamic = ["version", "readme"]
dependencies = [
    "click==8.2.1",
    "jsonschema",
    "pyyaml",
    "psutil",
    "xdg",
    "starlette",
    "hypercorn",
    # `compression.zstd` is new in Python 3.14; `compression.py` falls back to
    # this on the 3.13 that Homebrew and most distributions still ship.
    "zstandard; python_version < '3.14'"
]

[project.scripts]
pype = "revng.pypeline.main:run"

[tool.setuptools.package-data]
revng = [
    "pypeline/schema.yml",
    "pypeline/pipeline-description-schema.yml",
    "pypeline/storage/local_provider_migrations/v1.sql",
    "pypeline/storage/local_provider_migrations/v2.sql",
    "pypeline/storage/local_provider_migrations/v3.sql",
    "pypeline/rss_server/postgres_migrations/v1.sql",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.dynamic]
version = {attr = "revng.pypeline.__version__"}
readme = {file = ["README.md"], content-type = "text/markdown"}
